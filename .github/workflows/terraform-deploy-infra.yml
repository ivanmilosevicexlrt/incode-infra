name: Terraform

on:
  workflow_dispatch:
  pull_request:
    branches:
      - prod
  push:
    branches:
      - prod

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  plan:
    runs-on: ubuntu-latest
    environment: prod
    if: github.event_name == 'pull_request' || (github.event_name == 'push' && github.ref == 'refs/heads/prod') || github.event_name == 'workflow_dispatch'
    defaults:
      run:
        working-directory: prod
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.3

      - name: Terraform Init
        run: terraform init -backend-config=incode-ci.tfbackend --reconfigure

      - name: Terraform Plan
        run: terraform plan -var-file=envs/prod.tfvars -out=plan.out -no-color 2>&1 | tee plan.txt

      - name: Upload Plan Artifact
        uses: actions/upload-artifact@v4
        with:
          name: tfplan
          path: |
            prod/plan.out
            prod/plan.txt
            prod/.terraform
            prod/.terraform.lock.hcl
          retention-days: 1

      - name: Comment Plan on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const plan = fs.readFileSync('prod/plan.txt', 'utf8');
            const truncated = plan.length > 60000
              ? plan.substring(0, 60000) + '\n... (truncated)'
              : plan;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## Terraform Plan\n\`\`\`hcl\n${truncated}\n\`\`\``
            });

  infra-apply:
    needs: plan
    runs-on: ubuntu-latest
    environment: prod-apply
    if: github.event_name == 'push' && github.ref == 'refs/heads/prod' || github.event_name == 'workflow_dispatch'
    defaults:
      run:
        working-directory: prod
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.3

      - name: Download Plan Artifact
        uses: actions/download-artifact@v4
        with:
          name: tfplan
          path: prod/

      - name: Terraform Init
        run: terraform init -backend-config=incode-ci.tfbackend --reconfigure

      - name: Terraform Apply Infra
        run: terraform apply -auto-approve plan.out

  addons-apply:
    needs: infra-apply
    runs-on: ubuntu-latest
    environment: prod-addons
    defaults:
      run:
        working-directory: prod
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: us-east-1

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.14.3

      # 👇 Generate kubeconfig AFTER cluster is created
      - name: Configure kubeconfig for EKS
        run: |
          aws eks update-kubeconfig \
            --name prod-eks \
            --region us-east-1 \
            --kubeconfig /tmp/kubeconfig-prod-eks

      - name: Terraform Init (addons)
        run: terraform init -backend-config=incode-ci.tfbackend --reconfigure

      - name: Terraform Apply Addons
        run: terraform apply -auto-approve -var-file=envs/prod-addons.tfvars
